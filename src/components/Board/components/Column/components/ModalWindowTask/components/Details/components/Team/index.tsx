import React from 'react';

import s from './index.module.scss';

export const Team = () => {
    return (
        <div className={s.team}>
            <p className={s.team__title}>Team</p>
            <p className={s.team__name}>None</p>
        </div>
    );
};
