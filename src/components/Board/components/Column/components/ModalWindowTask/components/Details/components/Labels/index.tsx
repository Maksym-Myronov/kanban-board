import React from 'react';

import s from './index.module.scss';

export const Labels = () => {
    return (
        <div className={s.labels}>
            <p className={s.labels__title}>Labels</p>
            <p className={s.labels__name}>None</p>
        </div>
    );
};
